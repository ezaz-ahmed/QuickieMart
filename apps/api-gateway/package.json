{
  "name": "@quickie-mart/api-gateway",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "serve": {
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@quickie-mart/api-gateway:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@quickie-mart/api-gateway:build:development"
          },
          "production": {
            "buildTarget": "@quickie-mart/api-gateway:build:production"
          }
        }
      }
    }
  }
}
